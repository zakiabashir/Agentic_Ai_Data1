[project]
name = "guardrail"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "decouple>=0.0.7",
    "dotenv>=0.9.9",
    "openai-agents>=0.2.9",
    "python-decouple>=3.8",
]
